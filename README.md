## 🚀 Run_After_Save.py

Stop wasting your life Alt-Tabbing to the terminal. This script turns every `Ctrl+S` into an instant execution, so you can actually stay in the zone instead of wrestling with your keyboard shortcuts.

## 🛠️ Installation

This script uses the `watchdog` library to monitor file events efficiently without eating up your CPU.

```bash
pip install watchdog
